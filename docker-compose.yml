# Stack: Adom (torrents + debrid próprio) + Jackett.
# Antes de expor a stack, aponte adom.seudominio.com para a VPS e
# substitua o domínio no Caddyfile.
services:
  addon:
    build: .
    container_name: stremio-adom
    restart: unless-stopped
    ports:
      # Exposto na LAN pro Fire TV alcançar (127.0.0.1 só serviria a esta máquina).
      - "${PORT:-7000}:7000"
    env_file:
      - .env
    volumes:
      # Cache em SQLite: sem isso ele zera a cada rebuild e toda busca volta
      # a esquentar do zero.
      - ./docker-data/addon:/app/data
    environment:
      HOST: 0.0.0.0
      PORT: 7000
      JACKETT_URL: http://jackett:9117
      PROWLARR_URL: http://prowlarr:9696
    depends_on:
      - jackett
    networks:
      - adom

  jackett:
    build:
      context: ./jackett-bludv
    image: stremio-adom-jackett:local
    container_name: stremio-adom-jackett
    restart: unless-stopped
    environment:
      PUID: 1000
      PGID: 1000
      TZ: America/Sao_Paulo
      # Atualizações no container poderiam sobrescrever a definição local Bludv.
      # Atualize com `docker compose build --pull jackett && docker compose up -d jackett`.
      AUTO_UPDATE: "false"
    volumes:
      - ./docker-data/jackett:/config
      - ./docker-data/downloads:/downloads
    ports:
      - "127.0.0.1:9117:9117"
    # Sem depends_on no addon: ele é quem hospeda os resolvedores BR, mas a
    # chamada só acontece durante a busca — e o addon depende do jackett.
    networks:
      - adom

  # Resolve desafios Cloudflare para os indexers que exigem (1337x, eztv,
  # kickasstorrents). É um Chromium headless: pesa mais que o Jackett inteiro.
  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr:latest
    container_name: stremio-adom-flaresolverr
    restart: unless-stopped
    environment:
      LOG_LEVEL: info
      TZ: America/Sao_Paulo
    ports:
      - "127.0.0.1:8191:8191"
    # O padrão do Docker é 64 MB de /dev/shm e o Chromium usa isso como memória
    # compartilhada entre processos: em página pesada ele estoura e a aba morre
    # no meio do desafio, o que chega ao Jackett como "tracker fora do ar".
    shm_size: 1gb
    # Teto pra ele não levar o addon junto. O Chromium cresce a cada desafio e
    # esta é a única peça da stack que come RAM de verdade (~300 MB parado,
    # perto de 1 GB resolvendo). Estourando o teto, só ele é morto e o
    # `restart` acima o traz de volta — em vez do OOM killer escolher a vítima.
    mem_limit: 1536m
    networks:
      - adom

  prowlarr:
    image: lscr.io/linuxserver/prowlarr:latest
    container_name: stremio-adom-prowlarr
    restart: unless-stopped
    environment:
      PUID: 1000
      PGID: 1000
      TZ: America/Sao_Paulo
    volumes:
      - ./docker-data/prowlarr:/config
    ports:
      - "127.0.0.1:9696:9696"
    networks:
      - adom
    profiles:
      - full

  caddy:
    image: caddy:2-alpine
    container_name: stremio-adom-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - addon
    networks:
      - adom

networks:
  adom:
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
