node_modules/
# Saída do tsc: dist/src/, dist/test/, dist/scripts/.
dist/
.env
# Backups locais do .env: cópias com chaves reais não podem entrar no histórico.
.env.bak*
*.log
.DS_Store
Thumbs.db
docker-data/
# Cache SQLite quando o addon roda fora do container (CACHE_DB_PATH default).
data/
# Índice local da IDE Qoder.
.qoder/
jackett-config/
prowlarr-config/
.idea/
.vscode/
.kilo/
.agents/
# Configuração local de ferramenta de agente (mesmo critério de .kilo/.agents).
.claude/
