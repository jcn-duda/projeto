# O Dockerfile copia resolvers e definitions por caminho individual
# (bludv-resolver/server.js, jackett-bludv/*.yml, scripts/entrypoint.sh). A
# suíte Node não percebe um caminho quebrado: renomear um arquivo passa verde no
# CI e só falha no deploy. Este workflow existe só para quebrar antes disso.
#
# Os filtros de caminho espelham a lista de COPY do Dockerfile: src/, types/ e
# scripts/ alimentam o builder (tsc), e cada server.js/package.json de resolver
# entra na imagem de runtime. Mudança em qualquer um deles sem estar aqui é
# alteração de imagem que o workflow nunca vê.
#
# Workflow separado, e não um job no ci.yml, porque o filtro de caminho é por
# workflow. Rodar isto em todo push custaria caro à toa: o build completo faz
# apk add, pip install e a cópia do runtime .NET do Jackett.
name: Docker

on:
  push:
    paths:
      - 'Dockerfile'
      - 'docker-compose.yml'
      - 'scripts/**'
      - 'src/**'
      - 'types/**'
      - 'jackett-bludv/**'
      - 'bludv-resolver/server.js'
      - 'bludv-resolver/package.json'
      - 'comandotorrents-resolver/server.js'
      - 'comandotorrents-resolver/package.json'
      - 'nerdfilmes-resolver/server.js'
      - 'nerdfilmes-resolver/package.json'
      - 'torrentdosfilmes-resolver/server.js'
      - 'torrentdosfilmes-resolver/package.json'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - '.github/workflows/docker.yml'
  pull_request:
    paths:
      - 'Dockerfile'
      - 'docker-compose.yml'
      - 'scripts/**'
      - 'src/**'
      - 'types/**'
      - 'jackett-bludv/**'
      - 'bludv-resolver/server.js'
      - 'bludv-resolver/package.json'
      - 'comandotorrents-resolver/server.js'
      - 'comandotorrents-resolver/package.json'
      - 'nerdfilmes-resolver/server.js'
      - 'nerdfilmes-resolver/package.json'
      - 'torrentdosfilmes-resolver/server.js'
      - 'torrentdosfilmes-resolver/package.json'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - '.github/workflows/docker.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3

      - name: Construir imagem sem publicar
        uses: docker/build-push-action@v6
        with:
          context: .
          # Sem push e sem load: o objetivo é provar que o build passa. Carregar
          # a imagem no daemon só gastaria tempo e disco do runner.
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
